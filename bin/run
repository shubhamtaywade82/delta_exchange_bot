#!/usr/bin/env bash
# Canonical bot runtime lives in backend/. Root lib/bot is legacy-only; this script
# delegates to backend/bin/bot (Trading::Runner by default).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT/backend"
exec ./bin/bot "$@"
